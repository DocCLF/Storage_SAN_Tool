<UserControl x:Name="IBMTape"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    >
    <UserControl.Background>
            <LinearGradientBrush>
            <GradientStopCollection>
                <GradientStop Offset="0.1" Color="White" />
                <GradientStop Offset="0.5" Color="coral" />
                <GradientStop Offset="0.8" Color="White" />
            </GradientStopCollection>
        </LinearGradientBrush>
    </UserControl.Background>
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <GroupBox Header="Visibility" Margin="10" >
            <StackPanel Orientation="Horizontal" ToolTip="klick for visibility">
                <StackPanel x:Name="STP_TapeDeviceVisibility" >
                
                </StackPanel>
                <CheckBox x:Name="CB_SelectAllTapeCB" Content="Select All" Margin="22,2,2,2" Style="{DynamicResource MainCheckBoxStyle}" HorizontalAlignment="Right" />
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>