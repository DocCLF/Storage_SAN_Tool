<UserControl 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    >
    <UserControl.Background>
            <LinearGradientBrush>
            <GradientStopCollection>
                <GradientStop Offset="0.1" Color="White" />
                <GradientStop Offset="0.5" Color="LightGray" />
                <GradientStop Offset="0.8" Color="White" />
            </GradientStopCollection>
        </LinearGradientBrush>
    </UserControl.Background>
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox Grid.Column="0" Grid.RowSpan="4" Header="Actions" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0" >
            <StackPanel x:Name="SP_HMCCollector">
                <Button x:Name="BTN_HMCCollector" Content="HMCScaner" Style="{DynamicResource MainBtnStyle}">
                    <Button.ToolTip>
                         <ToolTip>
                             <TextBlock x:Name="BTN_HMCCollectorTooltip" Text="Only works in conjunction with the PRISMTool"/>
                         </ToolTip>
                    </Button.ToolTip>
                </Button>
            </StackPanel>
        </GroupBox>
        <ScrollViewer Grid.Column="1" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <WrapPanel Grid.Row="0" x:Name="HMC"></WrapPanel>
                <WrapPanel Grid.Row="1" x:Name="SYS"></WrapPanel>
                <WrapPanel Grid.Row="2" x:Name="LPAR"></WrapPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>