<UserControl 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    >
    <UserControl.Background>
            <LinearGradientBrush>
            <GradientStopCollection>
                <GradientStop Offset="0.1" Color="White" />
                <GradientStop Offset="0.5" Color="coral" />
                <GradientStop Offset="0.8" Color="White" />
            </GradientStopCollection>
        </LinearGradientBrush>
    </UserControl.Background>
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Viewbox Stretch="Uniform" StretchDirection="Both">
            <Canvas Width="2840" Height="300" Margin="20,0">
                <Image Source="{Binding DataContext.SAN720, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Width="2840"
                   Height="300"
                   Stretch="Fill" />
            <!-- Hotspot 1 -->
            <Border Canvas.Left="290"
                    Canvas.Top="126"
                    Width="77"
                    Height="36"
                    Background="Transparent"
                    Panel.ZIndex="10"
                    ToolTip="LAN-Anschluss">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="Orange"/>
                                <Setter Property="Background" Value="#30FFA500"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <!-- Hotspot 2 -->
            <Border Canvas.Left="290"
                    Canvas.Top="176"
                    Width="77"
                    Height="36"
                    Background="Transparent"
                    Panel.ZIndex="10"
                    ToolTip="USB-C">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="DeepSkyBlue"/>
                                <Setter Property="Background" Value="#3000BFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>
            </Canvas>
        </Viewbox>
    </Grid>
</UserControl>