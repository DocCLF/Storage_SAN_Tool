<UserControl 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    >
    <UserControl.Background>
        <LinearGradientBrush>
            <GradientStopCollection>
                <GradientStop Offset="0.1" Color="White" />
                <GradientStop Offset="0.5" Color="LightGray" />
                <GradientStop Offset="0.8" Color="White" />
            </GradientStopCollection>
        </LinearGradientBrush>
    </UserControl.Background>
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" >
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Row="0" Grid.Column="0" Header="Device Credentials" MinWidth="820" HorizontalAlignment="Left" Margin="20,10,0,0"  VerticalAlignment="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" x:Name="STP_GetDeviceCred" Orientation="Horizontal" Margin="2" >
                    <ComboBox x:Name="CB_DeviceType" SelectedIndex="0" SelectedValuePath="Content" Style="{DynamicResource MainComboBoxStyle}" >
                        <ComboBoxItem>IBM Storage</ComboBoxItem>
                        <ComboBoxItem>IBM Tape</ComboBoxItem>
                        <ComboBoxItem>Brocade SAN</ComboBoxItem>
                        <ComboBoxItem>PowerHMC</ComboBoxItem>
                    </ComboBox>
                    <Label x:Name="LB_Cluster"  Content="Cluster IP:" Style="{DynamicResource MainLabelStyle}"/>
                    <TextBox x:Name="TB_DeviceIPAddr" MinWidth="100" MaxLength="32" Style="{DynamicResource MainTextBoxStyle}" />
                    <Label  Content="UserName :" Style="{DynamicResource MainLabelStyle}"/>
                    <TextBox x:Name="TB_DeviceUserName" MaxLength="20" MinWidth="100" Style="{DynamicResource MainTextBoxStyle}" />
                    <Label Content="Password :" Style="{DynamicResource MainLabelStyle}" />
                    <PasswordBox x:Name="TB_DevicePassword" MaxLength="64" MinWidth="100" Style="{DynamicResource PasswordBoxStyle}" />
                    <Label x:Name="LB_SVCorVF" Style="{DynamicResource SVCorVFLabelStyle}" />
                    <CheckBox x:Name="CB_SVCorVF" VerticalAlignment="Center" Margin="2" >
                        <CheckBox.LayoutTransform>
                              <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                            </CheckBox.LayoutTransform>
                        <CheckBox.Style>
                            <Style TargetType="CheckBox">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=CB_DeviceType ,Path=SelectedValue}" Value="PowerHMC">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ElementName=CB_DeviceType ,Path=SelectedValue}" Value="IBM Tape">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>
                    <ToggleButton x:Name="BTN_SaveCredtoDG" Style="{DynamicResource AddUpdateToggleButtonStyle}" />
                </StackPanel>
                <StackPanel Grid.Row="1" x:Name="stp_storagePanel2" Orientation="Horizontal" Margin="2" >
                    <GroupBox Header="Systeme">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>
                            <DataGrid Grid.Column="0" x:Name="DG_KnownDeviceList" AutoGenerateColumns="False" Margin="10" MaxHeight="400" IsReadOnly="True" CanUserDeleteRows="True">
                                <DataGrid.Style>
                                    <Style TargetType="DataGrid">
                                        <Style.Triggers>
                                            <Trigger Property="HasItems" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"></Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.Style>
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DeviceTyp}" Value="Storage">
                                                <Setter Property="Background" Value="LightBlue" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding DeviceTyp}" Value="SAN">
                                                <Setter Property="Background" Value="LightYellow" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding DeviceTyp}" Value="PowerHMC">
                                                <Setter Property="Background" Value="LightGreen" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <!--<DataGridCheckBoxColumn Header="ACK?" Binding="{Binding ACKHosts, NotifyOnTargetUpdated=True, UpdateSourceTrigger=PropertyChanged}" /> -->
                                    <DataGridTextColumn Header="Device Typ" Width="Auto" Binding="{Binding DeviceTyp}" />
                                    <DataGridTextColumn Header="Connection Typ" Width="Auto" Binding="{Binding ConnectionTyp}" />
                                    <DataGridTextColumn Header="IP-Adresse" Width="Auto"  Binding="{Binding IPAddress}" />
                                    <DataGridTextColumn Header="Device Name" Width="Auto"  Binding="{Binding DeviceName}" />
                                    <DataGridTextColumn Header="Login User" Width="Auto"  Binding="{Binding UserName}" />
                                    <DataGridTextColumn Header="Login PW" Binding="{Binding Password}" Visibility="{Binding ElementName=Password, Path=IsChecked, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
                                    <DataGridTextColumn Header="Tape WWNN" Binding="{Binding TapeWWNN}" Visibility="{Binding ElementName=TapeWWNN, Path=IsChecked, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
                                    <DataGridTextColumn Header="MTM-Code" Width="Auto"  Binding="{Binding MTMCode}" />
                                    <DataGridTextColumn Header="Product" Width="Auto"  Binding="{Binding ProductDescr}" />
                                    <DataGridTextColumn Header="Firmware" Width="Auto"  Binding="{Binding CurrentFirmware}" />
                                    <DataGridTextColumn Header="SVC/ VF" Width="Auto" Binding="{Binding SVCorVF}" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Grid.Column="1" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal">
                                <CheckBox x:Name="CB_CredUpdate" Style="{DynamicResource MainCheckBoxStyle}" />
                                <Label Content="Credentials Update" Style="{DynamicResource MainLabelStyle}" />
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </StackPanel>
            </Grid>
        </GroupBox>
        <GroupBox Grid.RowSpan="4" Grid.Column="1" Header="Settings" HorizontalAlignment="Left" Margin="10,10,20,0" VerticalAlignment="Top">
            <StackPanel >
                <GroupBox Header="Customer Info" Margin="0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" Content="Customer Number" Style="{DynamicResource MainLabelStyle}" HorizontalAlignment="Left" />
                        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
                            <TextBox x:Name="TB_CustomerInfoName" Text="{Binding ElementName=CB_DataBaseChoice, Path=SelectedValue, Mode=OneWay}" HorizontalContentAlignment="Center" Width="100" Style="{DynamicResource ExportCredTBStyle}" HorizontalAlignment="Left" />
                            <ComboBox x:Name="CB_DataBaseChoice" SelectedIndex="0" Margin="10,0,0,0" Width="200" Style="{DynamicResource CustomerBoardCBStyle}" />
                            <ToggleButton x:name="BTN_DBRefresh" Margin="10,0,0,0" Height="32" Width="32" HorizontalAlignment="center" Background="Transparent" BorderThickness="0" Style="{DynamicResource CustomerBoardBTNStyle}">
                                <Image Source = "{Binding RefreshIcon96 , Mode=OneWay}" Stretch="Uniform" VerticalAlignment="Bottom" HorizontalAlignment="Left" MaxHeight="30" />
                            </ToggleButton>
                        </StackPanel>
                    </Grid>
                </GroupBox>
                <GroupBox Header="Credentials" Margin="0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <ToggleButton x:Name="BTN_ExportCred" Content="Export Credentials" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}" ToolTip="Credentials can only be used on this host with this user account!"/>
                            <ToggleButton x:Name="BTN_ImportCred" Content="Import Credentials" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}"/>
                            <CheckBox x:Name="CB_OnlineCheckbyImport" Margin="22,2,2,2" Style="{DynamicResource MainCheckBoxStyle}" />
                            <Label Content="Device Online check by import" Style="{DynamicResource MainLabelStyle}" />
                        </StackPanel>
                        <Label Grid.Row="1" x:Name="LB_CerdExportPath" Content="Empty" Style="{DynamicResource MainLabelStyle}" HorizontalAlignment="Left" />
                    </Grid>
                </GroupBox>
                <GroupBox Header="Export Settings" Margin="0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="Export Path :" Style="{DynamicResource MainLabelStyle}"/>
                        <TextBox Grid.Column="1" x:Name="TB_ExportPath" Text="" MinWidth="300" MaxWidth="450" HorizontalAlignment="Left" HorizontalContentAlignment="Left" Style="{DynamicResource MainTextBoxStyle}"/>
                        <ToggleButton Grid.Column="2" x:Name="BTN_ChangeExportPath" Content="Change Export Path" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}"/>
                    </Grid>
                </GroupBox>
                <!-- The ssh settings are deactivated for the time being and a better implementation should be sought. -->
                <GroupBox Header="Database Settings" Margin="0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Label Grid.ColumnSpan="2" Grid.Row="0" x:Name="LB_DBSettings" Visibility="Collapsed" Content="" HorizontalAlignment="Left" Height="30" Style="{DynamicResource MainLabelStyle}" />
                        <ToggleButton Grid.Column="0" Grid.Row="1" x:Name="BTN_ActivateDB" Content="Use DB" ToolTip="Red means that no Coustomer Name/Number has been assigned and no data will be saved!" HorizontalAlignment="Left" Style="{DynamicResource ExportCredBTNStyle}"/>
                        <ToggleButton Grid.Column="1" Grid.Row="1" x:Name="BTN_DeleteDB" Visibility="Collapsed" Content="Delete DB" ToolTip="Deletes the local DB file with all data content!" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}"/>
                    </Grid>
                </GroupBox>
                <CheckBox x:Name="CB_PRISMConnectOnOff" Content="PROFI PRISM" Style="{DynamicResource MainCheckBoxStyle}"/>
                <GroupBox Header="PROFI PRISM Connect Settings" Margin="0,10" Visibility="{Binding ElementName=CB_PRISMConnectOnOff, Path=IsChecked, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
                            <Label x:Name="LB_CustomerAZPPRISM" Content="Enter the PRISM connection details" Margin="0" Style="{DynamicResource PRISIMLabelStyle}" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Vertical" >
                            <StackPanel Orientation="Horizontal" >
                                <TextBox x:Name="TB_CustomerAZPPRISMConString" Tag="Az-ConnectionString" MaxLength="300" Width="200" HorizontalAlignment="Left" Margin="5,3" HorizontalContentAlignment="Left" Style="{DynamicResource TBAZWaterMarkStyle}"/>
                                <TextBox x:Name="TB_CustomerAZPPRISMDB" Tag="Az-SQL-DB-Name" Width="200" MaxLength="24" HorizontalAlignment="Left" Margin="5,3" HorizontalContentAlignment="Left" Style="{DynamicResource TBAZWaterMarkStyle}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" >
                                <TextBox x:Name="TB_CustomerAZPPRISMUM" Tag="UserManager-Name" Width="200" MaxWidth="200" MaxLength="24" HorizontalAlignment="Left" Margin="5,3" HorizontalContentAlignment="Left" Style="{DynamicResource TBAZWaterMarkStyle}"/>
                                <PasswordBox x:Name="TB_CustomerAZPPRISMUMP" Tag="Enter password" Width="200" MaxWidth="200" MaxLength="24" HorizontalAlignment="Left" Margin="5,3" HorizontalContentAlignment="Left" Style="{DynamicResource WatermarkPasswordBox}"/>
                            </StackPanel>
                            <ToggleButton x:Name="BTN_SaveAZConnectionPRISM" Content="Save" HorizontalAlignment="Left" VerticalAlignment="Bottom" Style="{DynamicResource ExportCredBTNStyle}"/>
                            <ToggleButton x:Name="BTN_ChangeAZConnectionPRISM" Content="Change Connection" Visibility="Collapsed" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Orientation="Vertical" >
                            <Label Grid.Column="0" Grid.Row="2" x:Name="LB_TestConnectionPRISM" Content="Connect to PROFI PRISM." Style="{DynamicResource PRISIMLabelStyle}" />
                            <ToggleButton Grid.Column="0" Grid.Row="3" x:Name="BTN_ConnetionToPRISM" Content="Test Connect" ToolTip="Test connection to PROFI PRISM" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}" />
                        </StackPanel>
                        <StackPanel Grid.Row="3" Orientation="Vertical" >
                            <Label Grid.Column="0" Grid.Row="2" x:Name="LB_SendDataToPRISM" Content="Send Data to PROFI PRISM." Style="{DynamicResource PRISIMLabelStyle}" />
                            <StackPanel Orientation="Horizontal" >
                                <ComboBox x:Name="CB_SQltoAzDB" SelectedIndex="0" Margin="10,0,0,0" Width="200" Style="{DynamicResource MainComboBoxStyle}" >
                                    <ComboBoxItem Tag="StorageBase">IBM Storage Base</ComboBoxItem>
                                    <ComboBoxItem Tag="StorageDrive">IBM Storage Drive</ComboBoxItem>
                                    <ComboBoxItem Tag="StorageEventLog">IBM Storage Events</ComboBoxItem>
                                    <ComboBoxItem Tag="PowerHMC">IBM Power HMC</ComboBoxItem>
                                    <ComboBoxItem Tag="PowerSysSummary">IBM Power SysSummary</ComboBoxItem>
                                    <ComboBoxItem Tag="LPARSummary">IBM Power LPARSummary</ComboBoxItem>
                                    <ComboBoxItem Tag="SANBase">Brocade SAN Base</ComboBoxItem>
                                    <ComboBoxItem Tag="LibraryBaseInfo">Library Base Info</ComboBoxItem>
                                    <ComboBoxItem Tag="LibraryDrive">Library Drive</ComboBoxItem>
                                    <ComboBoxItem Tag="LibraryEvents">Library Events</ComboBoxItem>
                                    <ComboBoxItem Tag="LibraryReports">Library Reports</ComboBoxItem>
                                </ComboBox>
                                <ToggleButton Grid.Column="0" Grid.Row="3" x:Name="BTN_SendDataToPRISM" Content="Send Data" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}" />
                            </StackPanel>    
                        </StackPanel>    
                    </Grid>
                </GroupBox>
                <GroupBox Header="Tool Settings" Margin="0,10">
                    <StackPanel Orientation="Horizontal">
                        <ToggleButton x:Name="BTN_SaveToolSettings" Content="Save Tool Settings" ToolTip="Red means that no Coustomer Number has been assigned and no data will be saved!" HorizontalAlignment="Left" Style="{DynamicResource ExportCredBTNStyle}"/>
                        <ToggleButton x:Name="BTN_LoadToolSettings" Content="Load Tool Settings" HorizontalAlignment="Left" Style="{DynamicResource MainBtnStyle}"/>
                        <CheckBox x:Name="CB_LoadSettingsatStartUp" Margin="22,2,2,2" Style="{DynamicResource MainCheckBoxStyle}" />
                        <Label Content="Load Settings at StartUp" Style="{DynamicResource MainLabelStyle}" />
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </GroupBox>
    </Grid>
    </ScrollViewer>
</UserControl>