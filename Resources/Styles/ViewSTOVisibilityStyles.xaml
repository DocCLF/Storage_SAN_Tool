<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Nur sichtbar, wenn Main.SelectedView == Base -->
    <Style x:Key="ShowWhenBase" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="Base">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <!-- Nur sichtbar, wenn Main.SelectedView == Events -->
    <Style x:Key="ShowWhenEvents" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="Events">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == CatAuditLog -->
    <Style x:Key="ShowWhenCatAuditLog" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="CatAuditLog">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == HostVolumeMap -->
    <Style x:Key="ShowWhenHostVolumeMap" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="HostVolumeMap">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == HostMap -->
    <Style x:Key="ShowWhenHost" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="HostMap">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == PoolVolumeInfo -->
    <Style x:Key="ShowWhenPoolVolumeInfo" TargetType="Grid">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="PoolVolumeInfo">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == DriveInfo -->
    <Style x:Key="ShowWhenDriveInfo" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="DriveMap">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == FCPortInfo -->
    <Style x:Key="ShowWhenFCPort" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="FCPort">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == FCPortStats -->
    <Style x:Key="ShowWhenFCPortStats" TargetType="Grid">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="FCPortStats">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == Partition -->
    <Style x:Key="ShowWhenPartition" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="Partition">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == Security -->
    <Style x:Key="ShowWhenSecurity" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="Security">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == UserInfo -->
    <Style x:Key="ShowWhenUserInfo" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="UserInfo">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == DumpInfo -->
    <Style x:Key="ShowWhenDumpInfo" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="DumpInfo">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Nur sichtbar, wenn Main.SelectedView == BackUpInfo -->
    <Style x:Key="ShowWhenBackUpInfo" TargetType="ItemsControl">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Main.SelectedView}" Value="BackUpInfo">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>